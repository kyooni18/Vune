name: Collection runtime immutable final pass

on:
  push:
    branches:
      - codex/collection-runtime-finalize-20260830

permissions:
  contents: write

jobs:
  final-pass:
    if: contains(github.event.head_commit.message, 'run immutable collection final pass')
    runs-on: ubuntu-24.04
    timeout-minutes: 50
    steps:
      - uses: actions/checkout@v4
        with:
          ref: codex/collection-runtime-finalize-20260830
          fetch-depth: 0
      - uses: pnpm/action-setup@v4
        with:
          version: 11.22.0
      - uses: actions/setup-node@v4
        with:
          node-version: 22.13.0
          cache: pnpm
      - name: Install dependencies
        run: pnpm install --frozen-lockfile
      - name: Immutable ordered validation
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p /tmp/vune-final
          pattern='row-local invalidation|flat keyed host rows|batches in-place ForEach mutations|compiled keyed collection|eventful compiled collection|push pop and reverse|duplicate occurrence identity'
          checks=(
            "build|pnpm run build"
            "core|node --test tests/core.test.mjs"
            "collection-web|node --test --test-name-pattern='$pattern' tests/web-package.test.mjs"
            "compiler|node --test tests/compiler-package.test.mjs"
            "react-vue|node --test --test-name-pattern='collection|ForEach|keyed' tests/react-client.test.mjs tests/vue-live.test.mjs"
            "full-web|node --test tests/web-package.test.mjs"
            "full-suite|pnpm test"
          )
          : > /tmp/vune-final/summary.txt
          failed=0
          failed_names=()
          for entry in "${checks[@]}"; do
            name=${entry%%|*}
            command=${entry#*|}
            if bash -lc "$command" > "/tmp/vune-final/$name.log" 2>&1; then
              echo "$name PASS" | tee -a /tmp/vune-final/summary.txt
            else
              echo "$name FAIL" | tee -a /tmp/vune-final/summary.txt
              failed=1
              failed_names+=("$name|$command")
            fi
          done
          if [ "$failed" -ne 0 ]; then
            git worktree add --detach /tmp/vune-checkpoint 71010c2b1c45ff5994eb6561d97e5e406e249d39
            (
              set +e
              cd /tmp/vune-checkpoint
              pnpm install --frozen-lockfile > /tmp/vune-final/checkpoint-install.log 2>&1
              pnpm run build > /tmp/vune-final/checkpoint-build.log 2>&1
              for entry in "${failed_names[@]}"; do
                name=${entry%%|*}
                command=${entry#*|}
                bash -lc "$command" > "/tmp/vune-final/checkpoint-$name.log" 2>&1
                echo "checkpoint-$name exit=$?" >> /tmp/vune-final/summary.txt
              done
            )
            git worktree remove --force /tmp/vune-checkpoint
            {
              cat /tmp/vune-final/summary.txt
              for file in /tmp/vune-final/*.log; do
                echo
                echo "===== $file ====="
                tail -n 240 "$file"
              done
            } > .github/collection-final-pass-failure.txt
            git config user.name github-actions[bot]
            git config user.email 41898282+github-actions[bot]@users.noreply.github.com
            git add -f .github/collection-final-pass-failure.txt
            git commit -m 'chore: record immutable collection final-pass failure'
            git push origin HEAD:codex/collection-runtime-finalize-20260830
            exit 1
          fi
      - name: Benchmark checkpoint and final source
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p /tmp/vune-validation
          git worktree add --detach /tmp/vune-checkpoint 71010c2b1c45ff5994eb6561d97e5e406e249d39
          (
            cd /tmp/vune-checkpoint
            pnpm install --frozen-lockfile
            pnpm run build
            node benchmarks/collection-runtime.mjs > /tmp/vune-validation/checkpoint.json
          )
          git worktree remove --force /tmp/vune-checkpoint
          node benchmarks/collection-runtime.mjs > /tmp/vune-validation/optimized.json
          echo 'Checkpoint benchmark:'
          cat /tmp/vune-validation/checkpoint.json
          echo 'Final benchmark:'
          cat /tmp/vune-validation/optimized.json
      - name: Remove temporary collection diagnostics
        shell: bash
        run: |
          set -euo pipefail
          git rm -r --ignore-unmatch \
            .github/collection-state-hotpath.mjs \
            .github/collection-runtime-apply.mjs \
            .github/collection-runtime-finalize.patch.gz.b64.* \
            .github/collection-runtime-patch \
            .github/collection-web-failure.txt \
            .github/collection-bisect-result.txt \
            .github/collection-recover-failure.txt \
            .github/collection-audit-failure.txt \
            .github/collection-target-pass.txt \
            .github/collection-target-failure.txt \
            .github/collection-full-failure.txt \
            .github/collection-key-recovery-pass.txt \
            .github/collection-key-recovery-failure.txt \
            .github/collection-final-pass-failure.txt \
            .github/workflows/collection-runtime-finalize.yml \
            .github/workflows/collection-runtime-fix.yml \
            .github/workflows/collection-runtime-validate.yml \
            .github/workflows/collection-runtime-autofinalize.yml \
            .github/workflows/collection-runtime-recover.yml \
            .github/workflows/collection-runtime-audit.yml \
            .github/workflows/collection-runtime-target.yml \
            .github/workflows/collection-runtime-full.yml \
            .github/workflows/collection-runtime-key-recovery.yml \
            .github/workflows/collection-runtime-final-pass.yml
          while IFS= read -r path; do
            case "$path" in
              .github/collection-*|.github/workflows/collection-*)
                echo "unexpected temporary collection diagnostic remains: $path" >&2
                exit 1
                ;;
            esac
          done < <(git ls-files '.github/**')
          git diff --cached --check
      - name: Commit immutable validated tree
        shell: bash
        run: |
          set -euo pipefail
          checkpoint=$(node -e "const fs=require('fs');process.stdout.write(JSON.stringify(JSON.parse(fs.readFileSync('/tmp/vune-validation/checkpoint.json','utf8'))))")
          optimized=$(node -e "const fs=require('fs');process.stdout.write(JSON.stringify(JSON.parse(fs.readFileSync('/tmp/vune-validation/optimized.json','utf8'))))")
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git commit \
            -m 'chore: finalize validated collection runtime' \
            -m "checkpoint-benchmark=$checkpoint" \
            -m "optimized-benchmark=$optimized" \
            -m 'validation=build,core,collection-web,compiler,react-vue,full-web,pnpm-test'
          git push origin HEAD:codex/collection-runtime-finalize-20260830
